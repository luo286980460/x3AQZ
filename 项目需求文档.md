# 尤特斯安全桩系统 — 项目需求文档

> 版本：v1.0　|　整理日期：2026-09-02
> 涉及子系统：`AQZServer`（安全桩主控服务 v0.5.0）、`OnBon`（仰邦控制卡控屏服务 v0.1.0）

---

## 1. 项目概述

本项目是一套部署于道路现场的 **交通安全「安全桩」设备** 软件系统，由厂商「尤特斯」提供。系统通过一台现场主机（Linux 嵌入式平台）对周边多种硬件传感器与外设进行集成，实现 **车辆测速 → LED 屏节目联动 → 语音播报 → 状态心跳上报** 的闭环业务流程。

整个系统在软件上拆分为两个独立可执行的 Qt 服务，分别承担不同的职责：

| 服务 | 名称 | 版本 | 端口 | 职责 |
| --- | --- | --- | --- | --- |
| AQZServer | 尤特斯安全桩服务 | 0.5.0 | 23331 | 主控编排服务：集成雷达、GPS、I2C 传感器、多端检测、HTTP 控制接口、心跳上报，并对外转发屏控/语音指令 |
| OnBon | 尤特斯仰邦控制卡控屏服务 | 0.1.0 | 23332 | 串口控屏服务：通过 Onbon 双 SDK（bx_sdk_dual）经串口对 LED 屏下发动态区/静态区节目与亮度调节 |

此外系统还依赖两个本地 HTTP 微服务：**APlayer（语音播放，端口 8888）** 与 **Kafka 生产者代理（端口 23333）**，由外部进程提供。

---

## 2. 项目背景

- 安全桩为部署于道路（含隧道、施工段等场景）的提示性设备，需根据车辆通行状态实时改变 LED 屏显示内容并播报语音。
- 需支持 **单端独立工作** 与 **多端（主机/从机）联动** 两种模式，主机将节目数据通过串口同步给从机，实现多块屏协同显示。
- 需根据环境光强度自动调节屏幕亮度与语音音量，并周期性将设备电压/电流/功率/光照/GPS 等状态上报到后台 Kafka。
- 外部管控平台可通过 HTTP 接口对设备进行远程管控（下发强制显示节目、关闭管控）。
- 相关说明文档：`x3安全桩配置文件说明.docx`、`x3安全桩上传kafka文件说明.docx`、`6E3P控制协议v1.0.docx`、`6E3P配置文件说明.docx`。

---

## 3. 功能需求

### 3.1 AQZServer 主控服务

#### 3.1.1 HTTP 控制接口（FR-A-01）
系统需对外提供基于 HTTP/JSON 的控制 API，供外部管控平台调用：
- `POST /screen/setDefaultProgam`：设置默认节目。
- `POST /screen/openControl`：开启管控，下发强制节目（优先级最高）。
- `POST /screen/closeControl`：关闭管控，恢复默认节目。
- `GET /ping`：返回服务名/版本/更新时间，用于存活探测。
- `GET /paths`：返回已注册路由列表。
- 静态文件服务：自动扫描 `./src/dist` 目录并对外暴露其中的文件。
- 所有 POST 业务接口要求请求头 `Content-Type: application/json`，否则返回 400。
- 提供 AES-128-ECB-PKCS7 加解密能力（已封装，当前 `/ping` 等接口默认未启用密文返回）。

#### 3.1.2 雷达测速与节目联动（FR-A-02）
- 通过串口读取雷达数据（数据格式：方向符 `+`/`-` + 速度数值）。
- 解析车辆方向（来/去）与速度，并按配置阈值 `speedingThreshold` 判定是否超速。
- 根据超速状态更新屏幕 **上半部分（速度数值）** 与 **下半部分（正常/超速驾驶文案）** 节目。
- 超速进入时触发超速语音播报，超速退出时停止语音（带延迟关闭）。
- 同步将「多端节目数据」更新到检测模式模块，用于主机→从机联动。

#### 3.1.3 多端检测模式（FR-A-03）
- 支持三种模式：`0=主机`、`1=从机`、`2=不做多端`（关闭）。
- 主机周期性（`loopTimer`）通过串口向从机写入 JSON 节目数据；从机接收后播放对端节目。
- 主机/从机通过 JSON 中的 `reader` 字段区分收发身份，防止回环。
- 从机收到对端节目后通过信号触发本地节目播放。

#### 3.1.4 I2C 传感器采集与联动（FR-A-04）
- 采集 **BH1750 光照传感器**（地址 0x23）的 Lux 值，做滑动平均（窗口 `times`）。
- 采集 **INA226 电压/电流/功率监测芯片**（地址 0x40）：总线电压、分流电压、电流、功率，并按电流区间做电压补偿修正。
- 按 `luxLevel` 配置区间映射出 **屏幕亮度** 与 **语音音量**，仅在变化时下发给 OnBon（亮度）与 APlayer（音量）。
- 采集结果同步到心跳数据，随心跳上报后台。

#### 3.1.5 GPS 定位（FR-A-05）
- 通过串口读取 NMEA 报文，解析 `$BDGGA`（北斗 GGA）语句。
- 提取纬度（N）与经度（E），写入心跳数据上报后台。

#### 3.1.6 节目优先级管理（FR-A-06）
系统需对屏幕节目来源进行优先级仲裁，低优先级不得覆盖高优先级节目：
- 优先级顺序（由高到低）：`DEFAULT_PROGRAM < RADAR < CONTROL < OTHER_LUA`（管控 / 其他端节目优先）。
- 管控开启时不回退默认节目；雷达/其他端节目触发会重置「恢复默认节目」计时。

#### 3.1.7 定时任务（FR-A-07）
- 周期 1 秒的心跳计时：
  - 每 `heartBeatInterval` 秒向本地 Kafka 代理（`/kafka/producer`）发送一次心跳，包含设备号 `sbbh`、管控状态、时间、电压/电流/功率/光照/GPS。
- 「恢复默认节目」计时：达到 `back2DefaultProgramTime` 秒后恢复默认节目并停止语音。
- 延迟关闭语音计时：达到设定延迟后向 APlayer 发送停止播放。

#### 3.1.8 配置校验（FR-A-08）
- 启动时读取 `cfg.json`，对每个模块的字段逐项校验（存在性、类型、取值范围），缺失/非法则跳过对应模块并打印错误日志，不致整体崩溃。
- 关键校验示例：端口范围 1024–65535、波特率限定集合、I2C 地址（35/40）、检测模式枚举（0/1/2）、亮度 0–15、音量 1–10、颜色枚举（1红/2绿/3黄）。

#### 3.1.9 外发编排（FR-A-09）
- 通过 `QNetworkAccessManager` 以 HTTP POST 方式编排对外调用：
  - OnBon 控屏：`/screen/text2Dynamic`、`/screen/text2Static`、`/screen/setBrightness`、`/screen/closeDynamic`。
  - APlayer 语音：`/setVolume`、`/setLoop`、`/addAudio`、`/clearPlayList`、`/playIndex`、`/play/stop`。
  - Kafka：`/kafka/producer`。
- 启动时清空 APlayer 播放列表并重新加载配置的 `playList`。

### 3.2 OnBon 控屏服务

#### 3.2.1 HTTP 控屏接口（FR-B-01）
对外提供基于 HTTP/JSON 的控屏 API（默认端口 23332）：
- `POST /screen/text2Dynamic`：下发动态区文本节目（详细参数见 3.2.3）。
- `POST /screen/text2Static`：下发静态区文本节目。
- `POST /screen/setBrightness`：设置屏幕亮度（0–15）。
- `POST /screen/closeDynamic`：关闭指定动态区。
- `POST /screen/test`：测试接口（透传 JSON 到 Screen 测试槽）。
- `POST /screen/picToDynamic`：图片转动态区（**待实现**，当前返回「待实现」）。
- `GET /ping`：返回控屏服务名/版本/更新时间。
- 静态文件服务：扫描 `./src/dist` 暴露文件。
- 所有 POST 业务接口要求 `Content-Type: application/json`。

#### 3.2.2 串口控屏（FR-B-02）
- 通过配置的串口（如 `COM6`，Linux 需写完整路径如 `/dev/ttyXXX`）与 Onbon 控制卡通信。
- 初始化时调用 `bxDual_InitSdk()` 完成双 SDK 初始化，设置默认显示参数、区域属性与页面属性。
- 动态区下发：组装区域/字体/颜色/对齐/显示模式等参数，调用 `bxDual_dynamicArea_AddAreaTxtDetails_6G_Serial` 下发文本。
- 动态区关闭：按 `areaId` 调用 `bxDual_dynamicArea_DelArea_G5_Serial` 删除指定动态区。
- 静态区下发：构造 program/area/page header，调用 `bxDual_program_addProgram_G6`、`bxDual_program_addArea_G6` 等接口写入静态节目。
- 亮度调节：通过 SDK 设置屏幕亮度等级。

#### 3.2.3 动态区文本参数规范（FR-B-03）
`/screen/text2Dynamic` 必填参数及校验规则：

| 参数 | 类型 | 取值/说明 |
| --- | --- | --- |
| nBaudRateIndex | int | 1=9600 / 2=57600 |
| color | int | 1=红 / 2=绿 / 3=黄 |
| areaId | int | ≥0 |
| areaX / areaY | int | 区域坐标 |
| areaWidth / areaHeight | int | 区域宽高（如 64×256） |
| fontName | string | 字体名（如「等线」） |
| fontSize | int | 字号 |
| content | string | 显示文本 |
| Halign | int | 1=左 / 2=居中 / 3=右 |
| Valign | int | 1=上 / 2=居中 / 3=下 |
| DisplayMode | int | 0–2 显示模式 |
| Speed | int | ≥1 滚动速度 |

#### 3.2.4 默认节目自适配（FR-B-04）
AQZServer 侧 `parseScreenOpenControl` 根据文字数量与单/双屏自动选择字号并做排版（插空格/换行/左右交错），典型规则：
- 单屏：2/4/5/6/7/8/12/16 字；双屏：4/8/10/12/14 字。
- 违反规则返回 `code=1` 并提示可接受字数。

---

## 4. 非功能需求

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| NFR-01 | 可靠性 | 各模块独立初始化与校验，单模块配置失败不影响其他模块运行；串口打开失败仅打印日志不崩溃 |
| NFR-02 | 实时性 | 雷达数据触发即处理；I2C 采集周期可配（默认 3 秒）；心跳周期可配（默认 60 秒） |
| NFR-03 | 解耦性 | I2C 采集运行于独立线程，与主线程通过 Qt 信号槽通信，避免阻塞 UI/网络线程 |
| NFR-04 | 可配置性 | 全部模块参数通过 `cfg.json` 配置，支持端口号、串口、波特率、阈值、亮度/音量映射等 |
| NFR-05 | 可观测性 | 关键流程通过 `qDebug`/`qCritical` 输出日志；`/ping` 与 `/paths` 提供存活与路由自省 |
| NFR-06 | 安全性 | HTTP 接口强制 JSON Content-Type；内置 AES-128-ECB-PKCS7 加解密封装，可按需启用密文传输 |
| NFR-07 | 跨平台 | 编译环境 Qt 5.15.2 MinGW64；I2C/串口部分依赖 Linux（`/dev/i2c-*`、`/dev/tty*`），控屏串口在 Windows 下使用 `COMx` |
| NFR-08 | 容错 | 速度与上次相同不重复下发节目；亮度/音量未变化不重复下发；HTTP 外发失败仅打印日志不中断主流程 |

---

## 5. 接口需求汇总

### 5.1 AQZServer 对外 HTTP API（端口 23331）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /ping | 存活探测，返回服务名/版本 |
| GET | /paths | 路由列表 |
| GET | /get | 回显请求信息 |
| GET | /user/{id} | 路径参数示例 |
| GET | /async | 异步响应示例 |
| POST | /echo | 回显请求体 |
| POST | /screen/setDefaultProgam | 设置默认节目 |
| POST | /screen/openControl | 开启管控 |
| POST | /screen/closeControl | 关闭管控 |
| GET | /<静态文件> | 静态文件服务（src/dist） |

### 5.2 AQZServer 内部外发调用
| 目标 | 路径 | 用途 |
| --- | --- | --- |
| OnBon(23332) | /screen/text2Dynamic | 动态区节目 |
| OnBon(23332) | /screen/text2Static | 静态区节目 |
| OnBon(23332) | /screen/setBrightness | 设置亮度 |
| OnBon(23332) | /screen/closeDynamic | 关闭动态区 |
| APlayer(8888) | /setVolume /setLoop /addAudio /clearPlayList /playIndex /play/stop | 语音播报控制 |
| Kafka(23333) | /kafka/producer | 心跳上报 |

### 5.3 OnBon 对外 HTTP API（端口 23332）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /ping | 存活探测 |
| GET | /paths | 路由列表 |
| POST | /screen/text2Dynamic | 动态区文本节目 |
| POST | /screen/text2Static | 静态区文本节目 |
| POST | /screen/setBrightness | 设置亮度（0–15） |
| POST | /screen/closeDynamic | 关闭动态区 |
| POST | /screen/test | 测试接口 |
| POST | /screen/picToDynamic | 图片转动态区（待实现） |

### 5.4 硬件/外设接口
| 外设 | 接口 | 设备/地址 | 用途 |
| --- | --- | --- | --- |
| 雷达 | 串口 | /dev/ttyCH343USB1 @9600 | 测速与方向 |
| 检测模式 | 串口 | /dev/ttyCH343USB0 @9600 | 主从机联动 |
| GPS | 串口 | /dev/ttyCH343USB3 @115200 | 北斗定位 |
| BH1750 | I2C | /dev/i2c-3 @0x23 | 光照采集 |
| INA226 | I2C | /dev/i2c-3 @0x40 | 电压/电流/功率 |
| Onbon LED 屏 | 串口 | COM6（Linux 完整路径） | 屏幕显示 |

---

## 6. 配置需求

### 6.1 AQZServer `cfg.json` 字段
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| sbbh | string | 设备编号（如 x3AQZ01） |
| heartBeatInterval | int | 心跳周期（秒） |
| heartBeatTopic | string | 心跳 Kafka topic |
| onBonServerPort | int | OnBon 服务端口（23332） |
| kafkaServerPort | int | Kafka 代理端口（23333） |
| aPlayerServerPort | int | APlayer 端口（8888） |
| back2DefaultProgramTime | int | 恢复默认节目秒数 |
| speedingAudioId / speedingAudioIdTimes | int | 超速语音 ID 与播放次数 |
| luraAudioId / luraAudioIdTimes | int | 多端语音 ID 与播放次数 |
| HttpServer | object | port + defaultProgram{content,color,signalScreen} |
| Radar | object | work/portName/baudRate/speedingThreshold/ownProgram/otherProgram |
| I2CManager | object | interval/times/address/deviceName/luxLevel[] |
| DetectionMode | object | portName/baudRate/detectionMode/loopTimer |
| Gps | object | portName/baudRate |
| APlayer | object | port/playList[] |

### 6.2 OnBon `cfg.json` 字段
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| httpServer.port | int | 控屏服务端口（23332） |
| screen.portName | string | 控屏串口号（Linux 需完整路径） |

---

## 7. 运行环境需求

- 操作系统：Linux 嵌入式平台（I2C/串口依赖 Linux 设备文件）；控屏串口可在 Windows 下使用 `COMx`。
- 编译环境：Qt 5.15.2 + MinGW 64-bit。
- 依赖库：
  - libhv（HTTP 服务框架，已附带 `.so`/`.dll`/`.a`）。
  - Onbon 双 SDK `bx_sdk_dual`（含 cairo/freetype/libpng/libiconv/zlibwapi 等运行时依赖，仅 OnBon 使用）。
  - Qt 模块：`serialport`、`network`、`core`。
- 外部进程：APlayer（语音播放）、Kafka 生产者代理（HTTP 网关）。
- 目录约定：可执行文件同目录需存在 `cfg.json` 与 `src/dist` 静态资源目录。

---

## 8. 验收准则（摘要）

1. 各模块按 `cfg.json` 配置独立启动，缺项/非法项有明确日志且不崩溃。
2. 雷达触发后屏显速度与超速/正常文案正确切换，超速语音按次数播报并延迟关闭。
3. 多端主机/从机经串口正确收发节目，`reader` 身份校验生效。
4. I2C 周期采集光照/电压/电流/功率，并按区间调节亮度与音量。
5. 心跳按周期携带完整状态上报 Kafka。
6. HTTP 管控接口可远程开启/关闭管控，关闭后恢复默认节目。
7. OnBon 控屏接口可正确下发动态区/静态区节目、设置亮度、关闭动态区，参数校验对非法输入返回明确错误码。
