# 尤特斯安全桩系统 — 项目架构文档

> 版本：v1.0　|　整理日期：2026-09-02
> 涵盖子系统：`AQZServer`（主控编排，端口 23331）、`OnBon`（串口控屏，端口 23332）
> 外部依赖进程：`APlayer`（语音，端口 8888）、`Kafka HTTP 代理`（端口 23333）

---

## 1. 系统概述

本系统为道路现场「安全桩」设备的嵌入式软件，采用 **多服务 + 本地 HTTP 总线** 的架构：现场主机上运行若干独立进程，进程之间通过本地回环 HTTP（`127.0.0.1`）解耦通信，对外则只暴露 AQZServer 的控制端口。核心思路是「编排层 / 执行层分离」：

- **AQZServer** 作为编排层，负责传感器采集、业务决策、节目优先级仲裁，并把屏控/语音/心跳指令转发给对应执行层。
- **OnBon** 作为屏幕执行层，封装 Onbon 双 SDK，通过串口真正驱动 LED 屏。
- **APlayer** / **Kafka 代理** 为外部执行层，分别负责语音播放与后台消息上报。

```
                    +------------------- 现场主机 (Linux) -------------------+
                    |                                                          |
  外部管控平台 ---->|  AQZServer(23331)  --HTTP-->  OnBon(23332)  --串口-->  LED屏
  (HTTP/JSON)      |        |  --HTTP-->  APlayer(8888) --音频--> 扬声器        |
                    |        |  --HTTP-->  Kafka代理(23333) --> 后台Kafka集群    |
                    |        |                                                 |
                    |   串口:雷达/检测模式/GPS   I2C:BH1750+INA226            |
                    +----------------------------------------------------------+
```

---

## 2. 进程与服务划分

| 进程/服务 | 端口 | 角色 | 主要源码 |
| --- | --- | --- | --- |
| AQZServer | 23331 | 编排层（采集+决策+转发） | `AQZServer/` |
| OnBon | 23332 | 屏幕执行层（Onbon SDK 串口） | `OnBon/` |
| APlayer | 8888 | 语音执行层（外部进程） | — |
| Kafka 代理 | 23333 | 消息上报网关（外部进程） | — |

设计取舍：将「控屏」拆成独立进程而非合入 AQZServer，目的是隔离 Onbon SDK（C 动态库 + 字体/图形依赖）的稳定性风险，并复用同一控屏服务为多端安全桩提供能力。

---

## 3. AQZServer 内部架构

### 3.1 模块划分

```
                      MyMain（编排中心 / 定时器 / 心跳 / 优先级仲裁）
       _______________|__________________________________________________________
      |          |          |          |          |          |          |
 MyHttpServer Radar   DetectionMode  I2CManager   GPS     (外发HTTP)  (APlayer初始化)
   :23331     串口雷达    主从联动      I2C采集    串口NMEA
```

| 模块 | 源文件 | 职责 |
| --- | --- | --- |
| 入口 | `main.cpp` | 创建 `MyMain` 并进入 Qt 事件循环 |
| 编排中心 | `mymain.cpp/.h` | 读取 cfg、初始化各模块、定时任务（心跳/恢复默认/延迟停音）、节目优先级仲裁、HTTP 外发封装（OnBon/APlayer/Kafka） |
| HTTP 服务 | `myhttpserver.cpp/.h` | 基于 libhv 的 HttpService，注册路由、静态文件、AES 加解密、参数解析（`parseScreenOpenControl`） |
| 雷达 | `radar.cpp/.h` | 串口读取雷达，解析方向/速度，超速判定，触发上下屏节目与语音 |
| 检测模式 | `detectionmode.cpp/.h` | 串口主从通信，JSON 节目同步，从机播放对端节目 |
| I2C 管理 | `i2cmanager.cpp/.h` + `i2cmanagerworker.cpp/.h` | I2C 采集（BH1750/INA226），滑动平均，亮度/音量映射 |
| GPS | `gps.cpp/.h` | 串口 NMEA `$BDGGA` 解析，输出经纬度 |
| AES | `qaesencryption.cpp/.h` | AES-128/192/256 ECB/CBC/CFB/OFB + PKCS7/ISO 加解密 |

### 3.2 启动与初始化流程（`MyMain::init`）

1. `loadCfg`：读取 `cfg.json`，逐字段类型/范围校验（缺失即中止整体启动，单模块缺项则跳过该模块）。
2. 依次初始化（每步先 `xxxCfgIsOk` 校验再 `initXxx`）：
   HttpServer → Radar → I2CManager → DetectionMode → GPS → APlayer。
3. 建立跨模块信号槽：`Radar::signalUpdateDetectionModeProgram → DetectionMode::slotUpdateDetectionModeProgram`。
4. 启动 1 秒周期定时器 `initTimer`（心跳计数 / 恢复默认节目计数 / 延迟停音计数）。
5. 下发一次默认节目到 OnBon（清屏 + 恢复默认）。

### 3.3 节目优先级仲裁（核心机制）

枚举 `e_triggerScreenPriority`（值越小优先级越高）：

```
DEFAULT_PROGRAM(0) < RADAR(1) < CONTROL(2) < OTHER_LUA(3)
```

- 每个节目下发槽在执行前判断 `m_triggerScreenPriority < 当前源优先级` 则直接 return，避免低优先级覆盖高优先级节目。
- 管控开启：设为 `CONTROL`，且 `m_back2DefaultProgramTimeCount = back2DefaultProgramTime+1`（不回默认）。
- 雷达/其他端节目触发时重置「恢复默认节目」计数为 0。
- I2C 的亮度/音量调节不受节目优先级约束，按自身变化判定独立下发。

### 3.4 定时任务（1 秒 tick）

| 计数器 | 触发动作 |
| --- | --- |
| `m_heartBeatCount == heartBeatInterval` | 发心跳到 Kafka（sbbh/管控状态/时间/电压电流功率光照/GPS） |
| `m_back2DefaultProgramTimeCount == back2DefaultProgramTime` | 恢复默认节目 + 切回 DEFAULT_PROGRAM + 启动延迟停音 |
| `m_stopAudioLagCount == m_stopAudioLagValue` | 向 APlayer 发 `/play/stop` |

---

## 4. OnBon 内部架构

### 4.1 模块划分

```
            MyMainOnBon（加载配置 + 连接信号槽）
                    |
   __________________|____________________
  |                                     |
MyHttpServerOnBon(:23332)              Screen（Onbon 双 SDK 串口）
 libhv 路由 + 参数校验                  bxDual_InitSdk / 动态区 / 静态区 / 亮度
```

| 模块 | 源文件 | 职责 |
| --- | --- | --- |
| 入口 | `main.cpp` | 创建 `MyMainOnBon` 并进入事件循环 |
| 编排 | `mymainonbon.cpp/.h` | 读 cfg、校验端口、创建 HTTP 服务与 Screen、连接信号槽 |
| HTTP 服务 | `myhttpserveronbon.cpp/.h` | libhv 路由注册、参数校验、AES 封装、静态文件 |
| 屏幕控制 | `screen.cpp/.h` | 封装 `bx_sdk_dual`，串口下发动态区/静态区/亮度 |
| AES | `qaesencryption.cpp/.h` | 加解密（与 AQZServer 同源） |

### 4.2 控屏调用链

```
HTTP 请求 (/screen/text2Dynamic)
   -> MyHttpServerOnBon::parseScreenText2Dynamic（参数校验）
   -> emit signalDynamicArea_AddAreaTxtDetails_6G_serial(...)
   -> (Qt 信号槽, MyMainOnBon 连接)
   -> Screen::slotDynamicArea_AddAreaTxtDetails_6G_serial
   -> bxDual_dynamicArea_AddAreaTxtDetails_6G_Serial(...)  // 串口下发到 LED
```

静态区/亮度/关闭动态区同理，HTTP 层只做校验与信号转发，真正的 SDK 调用集中在 `Screen`，便于隔离。

---

## 5. 关键数据流

### 5.1 雷达测速 → 屏显 + 语音 + 多端同步

```
Radar.slotRead(串口数据)
  |– 解析方向(+/-) 与速度
  |– emit signalSendSpeedProgram2OnbonUp(speed,color)      -> MyMain -> OnBon /screen/text2Dynamic（上半屏速度）
  |– emit signalSendSpeedProgram2OnbonDown(文案,color)     -> MyMain -> OnBon /screen/text2Dynamic（下半屏文案）
  |– 超速进入: emit signalPlaySpeedingAudio                 -> MyMain -> APlayer /playIndex
  |– 超速退出: emit signalStopSpeedingAudio                 -> MyMain 延迟 -> APlayer /play/stop
  \– emit signalUpdateDetectionModeProgram(多端节目JSON)     -> DetectionMode 缓存, 主机定时写串口给从机
```

### 5.2 外部管控 → 强制节目

```
HTTP POST /screen/openControl
  -> MyHttpServer.parseScreenOpenControl(自动排版字号/单双屏)
  -> emit signalOpenControl(json, true)
  -> MyMain.slotOpenControl:
        setTriggerScreenPriority(CONTROL)
        OnBon /screen/closeDynamic（清屏）
        OnBon /screen/text2Dynamic（下发管控节目）
        m_controlState=true; 不回默认节目
```

### 5.3 I2C 采集 → 亮度/音量联动 + 心跳

```
I2CManagerWorker(独立线程, 周期 m_interval 秒)
  |– readLux() / read_bus_voltage() / get_current() / read_power()
  |– 滑动平均 Lux -> 按 luxLevel 映射 volume & screenBrightness
  |– emit signalUpdateI2CData(json)        -> MyMain 写入心跳字段
  \– emit signalUpdateVolumeAndBrightness -> MyMain:
        亮度变化 -> OnBon /screen/setBrightness
        音量变化 -> APlayer /setVolume
```

### 5.4 心跳上报

```
MyMain 1s 定时器 -> 达 heartBeatInterval
  -> 组装 {sbbh, controlState, time, busVoltage, shuntVoltage, current, power, Lux, gps}
  -> POST http://127.0.0.1:23333/kafka/producer {topic, key:"heartBeat", data}
```

---

## 6. 通信机制总览

| 通信路径 | 机制 | 说明 |
| --- | --- | --- |
| AQZServer ↔ OnBon / APlayer / Kafka | 本地 HTTP POST（QNetworkAccessManager，异步） | JSON 协议；失败仅日志不阻断 |
| AQZServer 模块内 | Qt 信号槽（同线程，队列安全） | Radar/DetectionMode/I2C/GPS/HttpServer 与 MyMain 互联 |
| I2CManager ↔ Worker | QThread + `moveToThread` | 采集在独立线程，结果通过信号回主线程 |
| AQZServer ↔ 雷达/GPS/检测模式 | Qt 串口 `QSerialPort`（`readyRead` 信号驱动） | 各自独立串口 |
| OnBon ↔ LED 屏 | 串口 + Onbon 双 SDK（`bx_sdk_dual`） | `bxDual_*` 系列函数 |
| I2C ↔ BH1750/INA226 | Linux `/dev/i2c-*` + `ioctl/read/write` | 直接系统调用 |
| 外部 ↔ AQZServer/OnBon | libhv HTTP（GET/POST） | 含静态文件、代理、AES |

---

## 7. 线程模型

```
+--- AQZServer 进程 ---+
| 主线程 (Qt event loop)        | libhv 内部线程 (HTTP 服务 23331)
|   - MyMain 定时器              |   - 处理 /ping /openControl ...
|   - Radar/DetectionMode/GPS    |
|     串口 readyRead 槽          |
|   - HTTP 外发 (QNetworkAM)     |
| I2C 独立线程                   |
|   - I2CManagerWorker 定时采集   |
+----------------------+
```

- AQZServer 的 libhv HTTP 服务自带线程池；HTTP 槽通过 `emit` 信号切回主线程的 `MyMain` 处理。
- I2C 采集明确隔离到 `I2CManagerWorker` 所在的独立 `QThread`，避免阻塞。
- OnBon 进程结构类似（主线程 + libhv 线程），但无独立采集线程。

---

## 8. 外部依赖

| 依赖 | 用途 | 引入方 |
| --- | --- | --- |
| libhv | HTTP 服务/路由/静态文件/异步 | AQZServer、OnBon（`include/libhv` + `libs/libhv`） |
| Onbon `bx_sdk_dual` | LED 控制卡串口通信 | OnBon（`include/libonbon` + `libs/libonbon`，含 cairo/freetype/libpng/libiconv/zlibwapi） |
| Qt serialport | 雷达/GPS/检测模式串口 | AQZServer |
| Qt network | HTTP 外发（QNetworkAccessManager） | AQZServer |
| linux/i2c-dev | I2C 系统调用 | AQZServer |
| APlayer / Kafka 代理 | 语音播放 / 消息上报 | 外部进程 |

---

## 9. 配置与目录约定

- 配置文件：可执行文件同目录 `cfg.json`（两个服务各有一份）。
- 静态资源：可执行文件同目录 `src/dist/`，启动时递归扫描并注册为 HTTP 静态文件路由。
- 关键路径宏：AQZServer 中 `CFG_JSON=/cfg.json`、`SRC_PATH=/src/dist`；OnBon 中 `SRC_PATH=/src/dist`、`LOGIN_PATH=/src/dist/index.html`。
- 串口路径：Linux 必须写完整路径（如 `/dev/ttyCH343USBx`），Windows 可用 `COMx`。

---

## 10. 架构特性与取舍

1. **本地 HTTP 总线**：进程间用回环 HTTP 解耦，便于独立部署/替换/重启（如 OnBon SDK 崩溃不影响 AQZServer 采集与心跳）。
2. **编排层集中仲裁**：所有节目下发都过 `MyMain`，统一做优先级判定与默认节目恢复，避免多源竞争屏幕。
3. **执行层薄封装**：OnBon 的 `Screen` 集中所有 `bxDual_*` 调用，HTTP 层只做校验+信号转发，SDK 升级/替换影响面可控。
4. **配置驱动 + 严格校验**：模块行为完全由 `cfg.json` 决定，启动期做类型/范围校验，单模块失败不拖垮整体。
5. **线程隔离**：唯一长耗时采集（I2C）独立线程化，其余串口/HTTP 均为事件驱动，主线程不被阻塞。
6. **可观测**：`/ping` 返回版本与更新时间，`/paths` 自省路由，关键节点 `qDebug/qCritical` 日志。

---

## 11. 已知限制 / 待完善

- `/screen/picToDynamic`（图片转动态区）在 OnBon 中尚未实现，当前直接返回「待实现」。
- AQZServer 与 OnBon 各自维护一份同源 `qaesencryption`，AES 密文传输当前默认未启用（代码已封装，注释状态）。
- `MyMain` 中 `slotPlayOtherLuaProgram` 末尾向 OnBon 下发的是原始 `json` 而非排版后的 `jsonDataP`（疑似遗留 bug，建议复核）。
- Kafka/APlayer 为外部进程，本仓库不含其实现，部署时需保证二者已在本机运行。
